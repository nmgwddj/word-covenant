{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Local-first desktop capability set. Network and shell access are intentionally absent.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
